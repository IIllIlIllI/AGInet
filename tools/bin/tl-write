#!/data/data/com.termux/files/usr/bin/bash

ROOT="$HOME/projects/truth-lens"
WRITER="$ROOT/tools/write-agent/writer.cjs"

if [ -z "$1" ]; then
  echo "Usage: tl-write <instructions.json>"
  exit 1
fi

node "$WRITER" "$1"
